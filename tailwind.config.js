const colors = require('tailwindcss/colors')

module.exports = {
    theme: {
        colors: {
            primary: '#FFE81F',
            black: colors.black,
            white: colors.white,
            gray: '#282727'
        }
    }
}
